#pragma once

#include <QMainWindow>

class MainWindow : public QMainWindow {

    
public:
    MainWindow(QWidget* _parent = nullptr, Qt::WindowFlags _flags = Qt::WindowFlags() );
    virtual ~MainWindow() {}


};